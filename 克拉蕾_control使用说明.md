# 克拉蕾_control.py 使用说明

给 `克拉蕾/克拉蕾.pmx` 做的简易角色控制脚本：**键盘实时控制行走/跳跃**，以及把动作**烘焙成关键帧**便于播放和渲染。

- 依赖：Blender 5.2.1 LTS + mmd_tools 扩展（`bl_ext.user_default.mmd_tools`）
- 脚本位置：`克拉蕾_control.py`

## 一、GUI 用法

1. 打开 Blender，切到 **Scripting** 工作区 → Open → 选 `D:\work\blender\克拉蕾_control.py` → **Run Script**。
   - 场景里没有骨架时，脚本会自动用 mmd_tools 导入 `克拉蕾.pmx`（scale 0.08，约 1.55 单位高）。
   - 已经有骨架（名字含"克拉蕾"）时直接复用，不重复导入。
2. 在 3D 视图按 **N** 打开侧边栏 → 选 **克拉蕾** 页签。
3. 面板上三组功能：

**键盘控制**（实时驱动，不产生关键帧）

| 按键 | 作用 |
| --- | --- |
| `W` / `S` | 前进 / 后退 |
| `A` / `D` | 左转 / 右转 |
| `Shift` | 加速跑（速度 ×1.8） |
| `空格` | 跳跃（下蹲 → 起跳 → 落地缓冲） |
| `Esc` | 退出控制 |

鼠标要停在 3D 视图内，按键才会被接收；进入控制时会清掉骨架上已有的 action。

**烘焙动作**（生成关键帧，写进 action）

- `行走` / `跑步` → action `克拉蕾_Walk`，2 个完整步循环，可无缝循环播放
- `跳跃` / `前跳` → action `克拉蕾_Jump`（原地起跳 / 边走边跳）
- 烘焙后会自动设置场景 `frame_start / frame_end`，直接按空格播放即可

**复位** — 清掉 action 并把所有骨骼姿势归零。

## 二、无界面自检

```powershell
& "D:\Program Files\Blender Foundation\Blender 5.2\blender.exe" -b --factory-startup `
  --python "D:\work\blender\克拉蕾_control.py" -- --selftest --render
```

- `--selftest`：烘焙行走与跳跃，并断言：支撑脚不打滑、摆动脚有抬起、身体向前位移、跳跃有下蹲/腾空/回落。
- `--render`：额外渲出预览帧到 `克拉蕾/render/control/`（`walk_*.png`、`jump_*.png`）。

最近一次实测结果：步循环 14 帧、速度 1.166 单位/秒；支撑脚漂移 0.0020、抬升 0.0000；跳跃 34 帧，下蹲 −0.100、最高 +0.410、落地误差 < 0.02。

## 三、参数调节

脚本顶部常量，单位为 Blender 单位（模型约 1.55 高，角色朝 **−Y**）：

- `STRIDE = 0.34` — 步幅（一次支撑相脚相对身体后移的距离）；根位移与它严格耦合，改大改小都不会打滑
- `FOOT_LIFT = 0.11` — 摆动腿抬脚高度
- `WALK_SPEED = 1.15` — 走路速度（单位/秒），`RUN_MULT = 1.8` 为跑步倍率
- `TURN_SPEED` — 转向角速度
- `BOB / SWAY / HIP_YAW / ARM_SWING` — 身体上下起伏、左右摆、髋部扭转、手臂摆幅
- `WALK_DIP = 0.047` — 行走时身体整体压低量。**别改到 0**：腿在静止姿态下几乎直立，身体不压低就没有 IK 余量，抬高会让支撑脚被拽离地面
- `JUMP_HEIGHT = 0.45`、`GRAVITY = 9.8`、`CROUCH_TIME/CROUCH_DEPTH`、`LAND_TIME` — 跳跃高度、重力与蓄力/落地缓冲

## 四、实现要点

- **腿靠脚部 IK**：写 `足ＩＫ.L/R` 的位移，mmd_tools 导入时建好的 IK 约束会自动解算 `ひざ`、`足首`；不去直接转大腿骨，避免和 IK 打架。
- **脚不打滑**：支撑相脚相对根骨匀速后移 `STRIDE`，同时根骨 `全ての親` 前进同样距离，两者由同一个 `stride_for(speed)` 计算，相位推进速率 = `|speed| / (2 × stride)`。
- **烘焙循环**：把每个步循环取整到整数帧，再反算速度对齐帧率，所以首尾帧姿态一致，可无缝循环；根骨位移曲线设为 LINEAR，避免循环接缝处减速。
- **跳跃是状态机**：`ground → crouch(蓄力下压) → air(vz 受重力积分，收腿) → land(缓冲) → ground`；腾空由根骨 Z 抬起整个角色（IK 脚是它的子级，会一起离地）。
- **姿势写入方式**：`Rig.move / Rig.rotate` 用骨骼 rest 矩阵做基变换（`M⁻¹ · R · M`），所以代码里给的都是"世界/角色空间"的直观量，不用手算 MMD 骨骼的局部轴。

主要骨骼（mmd_tools 命名）：`全ての親`（根）、`センター`（重心）、`下半身`、`上半身`、`足ＩＫ.L/R`、`腕.L/R`、`ひじ.L/R`。

## 五、已知限制

- 键盘实时控制依赖 GUI 事件循环，**无界面模式无法验证**，只在 GUI 里可用；已验证的是姿势计算与烘焙路径（两者共用同一套代码）。
- 没有地面碰撞检测，落地高度固定为 z = 0；斜坡/台阶需要自己改 `step()`。
- 手指、头部朝向、表情 morph 未参与控制。
- 走路是程序化生成的，不是动捕，风格偏"标准步态"；要更有个性得手 K 或导入 VMD 动作。

